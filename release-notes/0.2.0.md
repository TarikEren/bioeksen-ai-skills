# 0.2.0

Released 2026-09-17.

- bioeksen-sds-20260917T113455Z-2f8d feat(api)!: BREAKING CHANGE: the log aggregator's endpoints move from `HOST/api/logs` to `HOST/api/v1/logs`. No app implements them yet, so nothing is migrated
  - What changed: leave the four standard endpoints unversioned and serve every app-specific path under `/api/v{major}/`, bumped only by a breaking change, with both versions served for 90 days and an unrecognised version answered with 404 and `RES-4200`
- bioeksen-sds-20260917T121510Z-6b34 feat(auth)!: BREAKING CHANGE: app credentials no longer last 24 hours. Entra randomises access token lifetime between 60 and 90 minutes and does not expose it as a policy, so a lifetime MUST be read from `exp` and MUST NOT be assumed. Nothing implements the old rule yet
  - What changed: issue both principal types from Microsoft Entra ID, derive the principal type from `scp` and `roles` in place of a literal `typ` claim, name Keycloak as the documented fallback, and enumerate the configuration `CFG-5000` and `CFG-5001` refer to
- bioeksen-sds-20260917T053933Z-61892506 feat(api): a caller that sent neither `page` nor `limit` could not tell how much of the result set it was holding without hardcoding the defaults it was trying to avoid depending on
  - What changed: add `page` and `limit` to `filterParams`, in the prose and the schema
- bioeksen-sds-20260917T053933Z-4b5409f9 feat(api): `SYS-5500` exists for an app that is not yet ready to serve, but the endpoint most likely to be called during a deploy listed no 503
  - What changed: add the 503 response to `GET /api/admin/logs`, carrying the error envelope rather than the health schema
- bioeksen-sds-20260917T103651Z-d1373999 feat(logging): the correlation contract required every record of one request to share a `request=` value propagated between services, and no document named the header that carries it
  - What changed: name `X-Request-Id`, with the rules for adopting, propagating and echoing it, an opaque value format, and a prohibition on trusting it as input
- bioeksen-sds-20260917T103652Z-d9bd689e feat(api): nothing told a throttled caller when it could return, so each client picked its own interval and the ones that chose badly sustained the overload a 429 exists to shed
  - What changed: require `Retry-After` on every 429, in whole seconds rather than an HTTP date
- bioeksen-sds-20260917T113020Z-e4b7 feat(logging): `startDate` defaulted to "the earliest log record", which says nothing until a retention window exists, and no document said what happens to a `startDate` older than whatever the store holds
  - What changed: keep 90 days at the aggregator and 7 days in an app's own store, clamp an out-of-window `startDate` rather than rejecting it, and echo the clamped value in `filterParams`
- bioeksen-sds-20260917T125040Z-8c72 feat(api): `UP-5500`, `UP-5501` and `UP-5000` named the ways an outbound call fails and nothing said what a caller does next, so every service would have chosen its own timeout
  - What changed: add `service-calls.md` covering call classes, deadline propagation, retryability derived from the code ranges, retry budgets, circuit breaking and `Idempotency-Key`
- bioeksen-sds-20260917T125410Z-d740 feat(logging): every attempt of one logical call shares a `request=` value, so an operator reading three identical records could not tell a retry from three separate failures
  - What changed: add `attempt=<n>` to the structured tail, counting from 1
- bioeksen-sds-20260917T133310Z-7a58 feat(logging): the one endpoint every app in the estate calls carried its validation rules in prose alone, while the endpoints an app merely serves already had a schema
  - What changed: add `aggregator-api.yaml` as normative for all three endpoints, referencing the shared schemas rather than copying them, and reject a submission carrying `recordId`
- bioeksen-sds-20260917T151340Z-8fd2 feat(auth): one operator role was carried as an open decision, which read as a gap rather than a choice, and nothing recorded that the role grants both a capacity report and every app's logs
  - What changed: state the single role as deliberate, record the privilege gradient it accepts and the auditor whose arrival would justify splitting it, and require roles to be defined centrally with unknown ones ignored
- bioeksen-sds-20260917T053645Z-ff4fca27 fix(marketplace): the manifest named the same entity twice, and `author` is not a field the marketplace manifest defines
  - What changed: drop `author`, leaving `owner` to identify BioEksen
- bioeksen-sds-20260917T053724Z-65791a28 fix(auth): step 1 had drifted between the two documents that both claim to state it, one limiting failed authentications and the other a general request allowance, with nothing saying which wins
  - What changed: name both allowances in both documents, and mark `code-prefixes.md` normative for the order
- bioeksen-sds-20260917T053736Z-9ecb9a29 fix(logging): the worked example named `VAL-4000` for four invalid fields, a code that cannot apply once field validation has been reached, in the passage most likely to be copied into a handler
  - What changed: use `VAL-4001`, state what `VAL-4000` is reserved for, and define how the selection procedure runs over a request with several faults
- bioeksen-sds-20260917T053743Z-a7ac55f0 fix(logging): `SYS-5500` sat in the unconditional `CRITICAL` row, so every rolling deploy paged someone for a healthy release and taught operators that `CRITICAL` is noise
  - What changed: log not-ready as `ERROR` until the app's startup budget has elapsed, and `CRITICAL` once it has missed it
- bioeksen-sds-20260917T053755Z-3c37dff8 fix(api): paging claimed to be stable on a tie-break by `id`, which names the emitting app and is identical for every record this endpoint returns, so it resolved no ties at all
  - What changed: tie-break on the log store's own per-row identifier, which need not appear in the response
- bioeksen-sds-20260917T053801Z-b9cfa490 fix(logging): the by-id endpoint took an identifier that appeared in no listing, so there was no way to get from a page of results to a single record
  - What changed: return `recordId` on every listed record and name it in the sort rule
- bioeksen-sds-20260917T053806Z-47546e03 fix(api): the date parameters were typed ISO 8601 while the rest of the spec says RFC 3339, and ISO 8601 admits forms RFC 3339 rejects
  - What changed: type `startDate` and `endDate` as RFC 3339
- bioeksen-sds-20260917T103512Z-fc28b4ff fix(logging): `SKILL.md` required the `id` field to match the prefix of that app's error codes, which is unsatisfiable because prefixes name the kind of failure and one app emits several — and nothing said where an id comes from
  - What changed: define allocation as supplied by the project or the id-issuing service and asked for when absent, never guessed, and state that `id` bears no relation to a prefix
- bioeksen-sds-20260917T110240Z-7d1a fix(api): two documents still described the record's `id` as assigned by the emitting app after the allocation rule said the opposite, which reads as a licence to invent one
  - What changed: replace the restated field list with a pointer to the normative document, and correct the schema description
- bioeksen-sds-20260917T110415Z-b28c fix(logging): naming `X-Request-Id` left the code equality rule stranded under a heading about the header, where it rendered as a stray one-item list
  - What changed: move the rule back into the correlation list, with no wording change
- bioeksen-sds-20260917T142010Z-4a91 fix(api): the prose required `X-Request-Id`, `X-Request-Deadline-Ms` and `Idempotency-Key`, and none of the three appeared in either schema, so an implementer generating from the normative artifact produced a service that breaks the correlation contract
  - What changed: model the three request headers in both schemas, echo `X-Request-Id` on every response including errors, and return 429 with `Retry-After` wherever a credential is required
- bioeksen-sds-20260917T142615Z-1cf8 fix(api): three cross-file references resolved nowhere, and the aggregator's response tables omitted the statuses its own authorization section requires — one endpoint having no table at all
  - What changed: correct the three paths, and list every status the schema defines for each endpoint
- bioeksen-sds-20260917T151020Z-3b7e fix(logging): a pool that stays exhausted takes an app out of service as completely as an unreachable database, yet took `ERROR` from the general rule — while raising it outright would page someone for every burst of traffic
  - What changed: make it `ERROR` while transient and `CRITICAL` once sustained past 60 seconds, the conditional shape `SYS-5500` already uses
- bioeksen-sds-20260917T110744Z-c517 refactor(api): a single-element `allOf` existed only to hang a description beside a `$ref`, which OpenAPI 3.0 required and 3.1 does not
  - What changed: attach the description to the `$ref` directly, with no change to what validates
- bioeksen-sds-20260917T133015Z-51ad refactor(api): the aggregator schema would otherwise have copied the six record fields and the page wrapper, and invariant 1 only ever checks this file
  - What changed: split out `LogFields` and `LogPageBase`, rebuild `LogRecord` and `LogPage` on them, and use `unevaluatedProperties` because `additionalProperties` cannot see through `allOf`
- bioeksen-sds-20260917T053941Z-e36f5224 docs(api): the entry point named the four required endpoints as bare nouns, so a reader had to open a reference to learn their paths and which of them need a credential
  - What changed: inline the paths and the principal for each into `sds-api-design/SKILL.md`
- bioeksen-sds-20260917T053941Z-2d060590 docs: every skill link in the README resolved to a directory that does not exist, and adding a marketplace does not install anything from it
  - What changed: correct the paths to `plugins/bioeksen-sds/skills/` and add the install command
- bioeksen-sds-20260917T053941Z-a63979fc docs: an assistant editing this repository got nothing, because the rules that matter most are the ones no single file states and reconstructing them meant reading all fourteen documents
  - What changed: add `CLAUDE.md`, recording which document is normative where two overlap, the invariants that must hold, and the rule that error codes are never invented
- bioeksen-sds-20260917T103718Z-511e9dac docs: the repository was public with install instructions and no licence, and an absent licence is read by some as an invitation rather than a reservation of rights
  - What changed: add a proprietary licence reserving all rights, and scope the README's install steps to authorised parties
- bioeksen-sds-20260917T110538Z-4f60 docs(logging): four facts were written out in full in two places each, against this repository's own rule, and nothing in the checker covered any of them
  - What changed: keep each fact in one document and have the others name it
- bioeksen-sds-20260917T110652Z-9ae3 docs(auth): two of the three paragraphs arguing this validation order were near-verbatim copies of the argument in the file the table already names as normative
  - What changed: keep the expiry-before-signature argument, which is this table's own, and link for the rest
- bioeksen-sds-20260917T125245Z-f3b1 docs: three rules were written against a document that did not exist, leaving a backoff unbounded, a refresh lead time unstated, and a rate limit no caller had a way to respect
  - What changed: point each at `service-calls.md`, and give the aggregator's buffer the circuit breaker
- bioeksen-sds-20260917T133140Z-c9e2 docs(logging): the endpoint was written `:id` in a document whose next sentence explains that the parameter is not the `id` field
  - What changed: rename the path parameter to `:recordId`, matching the schema
- bioeksen-sds-20260917T121240Z-b7c3 docs(commit): the note must be committed before the tag while the entry count must equal the commits in the range, so the note's own commit falls inside that range with no entry and the count is short by one at every release
  - What changed: require the note to carry an entry for itself, minted before the commit is written, and record that the tag is `v{version}` where the file is `{version}.md`
- bioeksen-sds-20260917T142930Z-e3b6 docs: the repository's own descriptions had fallen behind it: one OpenAPI file counted where there are two, a Layout listing neither `release-notes/` nor `scripts/`, and a README covering only the serving half of `sds-api-design`
  - What changed: correct the counts and the layout, and name the aggregator schema and `service-calls.md` among the facts and where they live
- bioeksen-sds-20260917T140805Z-dd0b docs: `credentials.md` named `aggregator-api.md` bare, though that file belongs to `sds-logging`, so the one reference in the estate that crosses a skill boundary resolved only for a reader who already knew which skill owned it
  - What changed: write it relative to `skills/` as the layout rule requires, and give `plugin.json` a trailing newline and `sds-api-design` the sentence-ending period its three siblings carry
- bioeksen-sds-20260917T154510Z-2d0a build: the repository is subject to its own `sds-commit` skill and had no release note for any version, so the change record that skill requires did not exist
  - What changed: add the note for `0.2.0`, bump the plugin version in `plugin.json` and its mirror in `marketplace.json`, and record the practice as adopted
- bioeksen-sds-20260917T103705Z-03e41b49 ops: `CLAUDE.md` recorded three invariants and told the reader to check them by hand, and one of them had already drifted before anyone noticed
  - What changed: add `scripts/check_invariants.py` and run it on every push and pull request, comparing step conditions by code rather than by prose
- bioeksen-sds-20260917T103712Z-da0e7b82 ops: every git command printed a line-ending warning, and the tracked files disagreed with each other
  - What changed: store text as LF and check it out as CRLF, keeping shell scripts LF for the Linux runners
- bioeksen-sds-20260917T113620Z-a05e ops: nothing stopped a fifth path appearing in the schema, or an aggregator endpoint being written without its version, while every existing check still passed
  - What changed: assert the standard paths are exactly those four and unversioned, and that every aggregator endpoint carries `/api/v{major}/`
- bioeksen-sds-20260917T125520Z-0b9c ops: retry policy and the code registry state the same fact twice and can drift, and a range that quietly became retryable would triple the load of every unhandled error
  - What changed: assert the classification covers exactly the two server-fault ranges and calls only the 503 range retryable
- bioeksen-sds-20260917T133425Z-e061 ops: referencing across files replaces the risk of a drifting copy with a smaller one, and YAML that parses says nothing about whether its links land
  - What changed: resolve every `$ref` in both schema files, and require the two aggregator specs to document the same endpoints
- bioeksen-sds-20260917T121430Z-5d81 ops: the plugin version is written in two files, and a release that bumps one and forgets the other leaves the marketplace advertising a version that does not exist
  - What changed: assert the mirrored entry matches `plugin.json`, leaving the marketplace's own version free to move independently
- bioeksen-sds-20260917T103711Z-20c9c3f1 chore: a stray patch file from an interrupted staging session reached the repository root and was caught only by eye
  - What changed: ignore patch, editor and Python scratch files
